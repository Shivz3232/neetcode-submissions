class Solution {
    public int maxArea(int[] heights) {
        int result = 0;
        
        for (int i = 0; i < heights.length; i++) {
            int j = heights.length - 1;
            while (j > i) {
                
                j--;
            }
        }

        return result;
    }
}
